# Office rendering payload notices

Release packaging must replace the placeholders below with the exact
LibreOffice, Poppler, dynamic-library, and font payloads included in each
platform archive. Record the source URL, component version, applicable
license/notice text, target platform, and SHA-256 for every distributed file.

The checked-in repository contains no native binaries. This file covers only
the shared Office rendering payload; npm package licenses and the PPTX schema
notice are maintained with their respective source assets.
